namespace MVC_ParserTestApps
{
    partial class CarModels
    {
        public CarModels(string brand, string name, string carid) //model, pnames[i], pmodels[i]
        {
            this.BrandName = brand;
            this.ModelName = name;
            this.CarId = carid;
        }
    }
}